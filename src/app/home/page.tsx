import HeroSection from '@/components/HeroSection';

export default function HomePage() {
  return (
    <main className="bg-gradient-to-b from-green-100 to-teal-50 flex flex-col h-screen w-screen">
      <HeroSection />
    </main>
  );
}
